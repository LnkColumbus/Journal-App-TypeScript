import { createAsyncThunk } from '@reduxjs/toolkit';

export const createNote = createAsyncThunk('journal/createNote', async(obj, thunkApi) => {

});

export const getNotes = createAsyncThunk('journal/getNotes', async(obj, thunkApi) => {

});